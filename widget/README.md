# widget

Embeddable JavaScript chat widget package.

Planned responsibilities:
- Bootstrap bubble/panel UI
- Call `embed_session` and `chat` endpoints
- Render streaming answers and citations
- Handle blocked-origin, rate-limit, and quota states
